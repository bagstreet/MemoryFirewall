# Draft article — Memory Firewall

I evolved Continuum because a useful memory route can also become an attack path if recalled prose is allowed to drive tools. Memory Firewall treats retrieved records as untrusted data: it checks scope, expiry, provenance and instruction-shaped content, then uses, ignores, quarantines or escalates with a visible reason. The local red-team lab compares a permissive path with the firewall. **[Add factual ten-stage Mainnet run evidence after execution.]**
